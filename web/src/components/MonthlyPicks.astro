---
import FavoriteButton from './FavoriteButton';
import { getCategoryImage } from '../data/categoryImages';

interface Recipe {
  title: string;
  slug: string;
  cost: number;
  cook: string;
  servings: string;
  category: string;
}

interface Props {
  recipes: Recipe[];
}

const { recipes } = Astro.props;

const monthName = new Date().toLocaleString('en-US', { month: 'long' });

---

<section class="px-6 md:px-12 py-16 max-w-7xl mx-auto">
  <div class="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-4 mb-10">
    <div>
      <span class="inline-block px-3 py-1 bg-secondary-container text-on-secondary-container rounded-full text-xs font-black uppercase tracking-wider mb-3">
        Seasonal Spotlight
      </span>
      <h2 class="font-headline text-3xl md:text-4xl font-extrabold tracking-tight">
        What to Cook This Month ({monthName})
      </h2>
    </div>
    <a
      href="/recipes"
      class="inline-flex items-center gap-1.5 text-primary font-headline font-bold hover:underline whitespace-nowrap"
    >
      Browse All Recipes
      <span class="material-symbols-outlined text-lg" aria-hidden="true">arrow_forward</span>
    </a>
  </div>

  <div class="grid grid-cols-[repeat(auto-fill,minmax(280px,1fr))] gap-6 md:gap-8">
    {recipes.map((recipe) => (
      <a href={`/recipes/${recipe.slug}`} class="group block">
        <div class="bg-surface-container-lowest rounded-xl overflow-visible shadow-[0_8px_32px_rgba(47,47,47,0.04)] hover:-translate-y-1 hover:shadow-[0_24px_48px_rgba(0,0,0,0.08)] transition-all duration-300">
          <div class="p-3">
            <div class="relative h-44 md:h-56 w-full rounded-lg overflow-hidden bg-surface-container">
              <img src={`/assets/recipes/${recipe.slug.split('/').pop()}.webp`} alt="" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" aria-hidden="true" loading="lazy" data-fallback={getCategoryImage(recipe.category)} />
              <FavoriteButton client:idle recipeId={recipe.slug} />
            </div>
          </div>
          <div class="px-6 pb-6 pt-2">
            <h3 class="font-headline font-bold text-xl mb-2 group-hover:text-primary transition-colors line-clamp-2">
              {recipe.title}
            </h3>
            <div class="flex items-center gap-4 text-sm text-on-surface-variant">
              <span class="flex items-center gap-1">
                <span class="material-symbols-outlined text-base" aria-hidden="true">schedule</span>
                {recipe.cook}
              </span>
              <span class="flex items-center gap-1">
                <span class="material-symbols-outlined text-base" aria-hidden="true">payments</span>
                <span class="font-bold text-tertiary">${recipe.cost.toFixed(2)}</span>
              </span>
            </div>
          </div>
        </div>
      </a>
    ))}
  </div>
</section>
